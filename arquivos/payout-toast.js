(()=>{const toast=document.querySelector('.payout-toast[data-payouts]');if(!toast)return;let items=[];try{items=JSON.parse(toast.dataset.payouts)}catch(error){return}if(items.length<2)return;const name=toast.querySelector('[data-payout-name]'),amount=toast.querySelector('[data-payout-amount]');let index=0;const next=()=>{toast.classList.remove('is-changing');void toast.offsetWidth;toast.classList.add('is-changing');setTimeout(()=>{index=(index+1)%items.length;name.textContent=items[index].name;amount.textContent=items[index].amount},180)};setInterval(next,5200)})();
