(()=>{document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(button.dataset.copy);button.textContent='Copiado ✓';setTimeout(()=>button.textContent='Copiar',1800)}catch(e){const input=button.previousElementSibling;input?.select();document.execCommand('copy');button.textContent='Copiado ✓';setTimeout(()=>button.textContent='Copiar',1800)}}));const form=document.querySelector('form:has(input[name="action"][value="partner"])');if(form){const inputs=[form.elements.manager_percent,form.elements.influencer_percent],left=document.getElementById('budget-left'),box=left?.parentElement;const update=()=>{const sum=inputs.reduce((n,input)=>n+(Number(input.value)||0),0),remaining=70-sum,invalid=Math.abs(remaining)>.001;left.textContent=new Intl.NumberFormat('pt-BR',{maximumFractionDigits:2}).format(remaining)+'%';box.classList.toggle('invalid',invalid);form.querySelector('button.primary').disabled=invalid};inputs.forEach(input=>input.addEventListener('input',update));update()}})();
;document.querySelectorAll(".table-wrap table").forEach(table=>{const labels=Array.from(table.querySelectorAll("thead th"),cell=>cell.textContent.trim());table.querySelectorAll("tbody tr").forEach(row=>Array.from(row.cells).forEach((cell,index)=>cell.dataset.label=labels[index]||""));table.dataset.mobileReady="true";});
