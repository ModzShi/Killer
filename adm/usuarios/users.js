(()=>{const rows=[...document.querySelectorAll('#userRows tr')],search=document.getElementById('userSearch'),filters=[...document.querySelectorAll('[data-filter]')],empty=document.getElementById('noResults');let filter='all';const apply=()=>{const term=(search?.value||'').trim().toLocaleLowerCase('pt-BR');let visible=0;rows.forEach(row=>{const kind=filter==='all'||row.dataset.kind===filter||(filter==='blocked'&&row.dataset.blocked==='1'),match=!term||row.dataset.search.includes(term),show=kind&&match;row.hidden=!show;if(show)visible++});if(empty)empty.hidden=visible!==0};filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(item=>item.classList.remove('active'));button.classList.add('active');filter=button.dataset.filter;apply()}));search?.addEventListener('input',apply);const dialog=document.getElementById('userDialog'),money=value=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(value)||0);document.querySelectorAll('.usr-edit').forEach(button=>button.addEventListener('click',()=>{const user=JSON.parse(button.dataset.user),name=(user.nome||user.email.split('@')[0]).trim();document.getElementById('editId').value=user.id;document.getElementById('editName').value=name;document.getElementById('editEmail').value=user.email||'';document.getElementById('editPhone').value=user.telefone||'';document.getElementById('editBalance').value=Number(user.saldo)||0;document.getElementById('editCommission').value=Number(user.comissaofake)||0;document.getElementById('editPlan').value=Number(user.plano)||0;document.getElementById('editCpa').value=Number(user.cpa)||0;document.getElementById('editAffiliate').checked=String(user.afiliado_ativo)==='1';document.getElementById('editBlocked').checked=['1','true','on'].includes(String(user.bloc).toLowerCase());document.getElementById('editAvatar').textContent=name.charAt(0).toUpperCase();document.getElementById('editSummaryName').textContent=name;document.getElementById('editSummaryType').textContent=String(user.demo)==='1'?'Conta demo':'Conta de usuário';document.getElementById('viewDeposited').textContent=money(user.depositou);document.getElementById('viewWagered').textContent=money(user.total_apostado);document.getElementById('viewReferrals').textContent=user.indicados||'0';dialog.showModal()}));document.querySelector('[data-close]')?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',event=>{if(event.target===dialog)dialog.close()})})();
